# Python and editor files
__pycache__/
*.pyc
.venv/
.DS_Store
.env

# Generated by the scripts (rebuild with scripts/run_all.py and scripts/lease_charts.py)
output/
reports/

# Downloaded source documents stay local (some are copyrighted news pages).
# The manifests (URL + SHA-256 hash) are committed so others can get the same files.
raw/*
!raw/manifest.csv
!raw/leases/
raw/leases/*
!raw/leases/manifest.csv

# The cleanup script itself
cleanup_for_github.sh
